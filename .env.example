# Example environment variables
# Production values should be set in your hosting provider (Render, Heroku, etc.)

# Sidekiq web UI credentials
SIDEKIQ_WEB_USER=admin
SIDEKIQ_WEB_PASSWORD=change_this_to_a_strong_password

# Redis connection (use your managed Redis URL in production)
REDIS_URL=redis://127.0.0.1:6379/0

# Base URL for KeepAliveWorker pings
APP_BASE_URL=https://fisio-back.onrender.com
